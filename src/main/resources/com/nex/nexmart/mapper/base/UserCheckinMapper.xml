<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nex.nexmart.mapper.base.UserCheckinMapper">

    <select id="selectCheckedDaysOfMonth" resultType="java.lang.Integer">
        SELECT DAY(checkin_date)
        FROM user_checkin
        WHERE user_id = #{userId}
          AND YEAR(checkin_date) = #{year}
          AND MONTH(checkin_date) = #{month}
        ORDER BY checkin_date
    </select>
</mapper>
